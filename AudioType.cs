using UnityEngine;
using System.Collections;

// This is an enumeration type to represent each
// type of sound in Thralled.
public enum AudioType
{
    // Ambiance zones
    JUNGLE = 0,
    PLANTATION,
    CAVE,
    SENZALA,
    DUNGEON,
    MASTERS_HOUSE,
    SLAVE_SHIP,
    HEAVENWARD_TIDES,
    NONE,

    // Other AudioTypes
    OBJECT,
    PLAYER,
    BABY,
    RAIN,
    MUSIC
};
